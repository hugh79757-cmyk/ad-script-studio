{
  "name": "ad-script-studio",
  "version": "1.0.0",
  "description": "전략 제안서 생성기 + 영상 소스 생성기",
  "scripts": {
    "deploy": "vercel --prod",
    "deploy:preview": "vercel",
    "test:e2e": "open index.html && echo 'Run E2E_TEST.runAll() in browser console'",
    "test:review": "echo '1. vercel dev 2. POST /api/review 3. GET /api/review?id=xxx 4. PATCH /api/review'"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "vercel": "latest"
  }
}
