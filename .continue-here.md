# Continue Here — AD SCRIPT STUDIO

> 마지막 업데이트: 2026-08-01
> 세션에서 작업한 내용: PDF 한글 폰트 최적화 (CDN fetch → 로컬 서브셋 base64) + 배포 + 프로덕션 E2E 완료

---

## Current Position

- **프로젝트:** e-commerce 전략 제안서 생성기 (ad-script-studio)
- **Phase:** Phase 1-6 모두 완료 (Milestone v1 완료) + v1.0.1 폰트 최적화 배포 완료
- **Git:** `main` 브랜치, `origin` = `git@github.com:hugh79757-cmyk/ad-script-studio.git`
- **커밋:** `c799801` feat(pdf): 폰트 CDN fetch 제거 → 로컬 서브셋 base64 임베딩 + 10초 타임아웃 폴백
- **배포:** https://ad-script-studio.vercel.app (v1.0.1)

---

## Completed This Session

1. ✅ **폰트 CDN fetch 제거** — `pdf.js`의 `fetchKoreanFontBase64()`가 `raw.githubusercontent.com` 대신 같은 오리진 `fonts/NotoSansKR-subset.ttf`를 fetch (Vercel 정적 라우팅, HTTP 200 확인)
2. ✅ **서브셋 폰트 생성** — `fonts/NotoSansKR-subset.ttf` 1.1MB (원본 10.4MB 대비 89% 감소). KS X 1001 2,350자 + 추가 224자 + 자모 + 라틴/숫자 + KS X 1001 기호 988자(※① 등). wght=400 정적 인스턴스 (fvar/gvar/avar 제거 — jsPDF 호환)
3. ✅ **안전망** — AbortController 10초 타임아웃 + 기본 폰트 폴백 (폰트 404/타임아웃 시에도 PDF 생성 보장, `test-pdf-fallback.mjs`로 검증)
4. ✅ **로컬 검증** — 수동/자동 모드 텍스트 전 글리프 커버 (누락 0), 한글 5,637자 완전 일치, PDF 817KB→325KB (60% 감소)
5. ✅ **배포 + 프로덕션 E2E** — `vercel --prod` 완료, 자동 모드 90초 생성 → PDF 316KB 다운로드 (61% 감소), 전 글리프 커버 (누락 0), pageerror 0건
6. ✅ **BACKLOG.md 생성** — bold 굵기 이슈 + 자동 모드 지연 (v1.1 후보)

---

## Remaining Work

- [ ] 프라이빗 전환: GitHub 레포 → Settings → Danger Zone → Make private
- [ ] Milestone v1 회고 (RETROSPECTIVE.md)
- [ ] v1.1: PDF bold 굵기 (BACKLOG.md 참조)

---

## v1 잔존 위험 (2026-08-01 갱신)

1. **서브셋 폰트 1.1MB** — 목표(300KB~1MB) 상한을 10% 초과. 원인: KS X 1001 기호 988자(※①②③ 등) 추가. 기호 포함 요구를 충족하는 최소 구성이라 수용했으며, 향후 기호만 별도 폰트로 분리하면 876KB까지 가능. [검증됨: 실제 1,100,152 bytes]
2. **자동 모드 API 생성 지연 (90초)** — 프로덕션 E2E에서 확인. 폰트 로드와 무관하며 v1.1 백로그에 기록 (BACKLOG.md).
3. **한글 bold 굵기 미표현** — 서브셋이 wght=400 정적 인스턴스라 bold 문장이 normal과 동일 굵기. v1 버그 아님(텍스트 무결성 정상), v1.1 백로그 기록 (BACKLOG.md).
4. **pdftotext 미설치** — PDF 텍스트 검증은 pypdf로 대체함 (동일 결과 확인). 문서화 완료.
5. **jsPDF CDN 의존** — jspdf.umd.min.js는 여전히 cdn.jsdelivr.net에서 로드 (v1.0.1 범위에서 유지). CDN 장애 시 PDF 생성 자체가 불가할 수 있음. v1.1 후보.

---

## Decisions

1. **Vercel KV 사용** — `@vercel/kv` 패키지로 파일 I/O 대체. `/tmp` 저장 방식 제거.
2. **crypto.randomBytes** — 프로젝트 ID 생성 방식 변경. 22자 랜덤 문자열 (128비트 엔트로피).
3. **관련성 검사** — Tavily 검색 결과에 제품명/브랜드명 미포함 시 빈 값 반환. Hallucination 방지.
4. **SSH 리모트** — HTTPS 토큰 권한 문제로 SSH로 전환.

---

## Bugs & Issues Discovered

- **[해결됨]** `gh repo create` 실패 — GITHUB_TOKEN에 `administration` 권한 없음
  - 증상: `GraphQL: Resource not accessible by personal access token`
  - 해결책: 웹에서 수동 생성 후 SSH 리모트 연결
  - 미해결 여부: 완료

- **[해결됨]** `git push` 403 에러 (HTTPS)
  - 증상: `Permission to hugh79757-cmyk/ad-script-studio.git denied`
  - 해결책: SSH 리모트로 전환 후 푸시 성공
  - 미해결 여부: 완료

---

## Environment Notes

- **OS:** macOS (darwin)
- **Shell:** zsh
- **Git 프로토콜:** SSH
- **GitHub 계정:** hugh79757-cmyk
- **Node.js:** .nvmrc 파일 없음
- **Vercel CLI:** 설치됨
- **@vercel/kv:** v3.0.0 설치됨 (deprecated 경고 존재하지만 정상 동작)

---

## Resume Instructions

```bash
# 1. 프로젝트 이동
cd /Users/twinssn/projects2/ad-script-studio

# 2. KV 환경변수 로컬로 가져오기
vercel env pull .env.local

# 3. 로컬 개발 서버 시작
vercel dev

# 4. 브라우저에서 테스트
#    - http://localhost:3000 접속
#    - 자동 조사 테스트
#    - 기획안 생성 → 검토 링크 테스트
#    - 시크릿 모드에서 독립 세션 테스트
```

---

## Uncommitted Changes

```
 M .gitignore   (git add + commit 필요)
```
