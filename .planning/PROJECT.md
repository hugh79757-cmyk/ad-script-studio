# PROJECT — AD SCRIPT STUDIO (이커머스 전략 제안서 생성기)

## Vision

이커머스 마케터/MD가 입력한 브랜드·상품·타겟·리뷰 정보를 기반으로 **"감이 아니라 논리로 만든" 전략 제안서**를 자동 생성하는 웹 기반 도구. 광고 대본은 그 제안서 안에 포함된 하나의 구성요소이며, 핵심 가치는 **제품별 맞춤 당위성 근거**와 **설득형 제안서 문서화**에 있다.

두 가지 핵심 도구:
1. **전략 제안서 생성기 (Strategic Proposal Generator)** — 문제 진단 + 전략 근거 + 광고 대본 + 스토리보드 + 기대효과를 포함한 클라이언트향 제안서 생성
2. **영상 소스 생성기 (Video Source Generator)** — 내부용 재료 도구. 대본을 씬 단위로 파싱하여 이미지/영상 생성 AI에 바로 쓸 수 있는 EN 프롬프트 생성

## Goals

### Validated (이전 세션 완료)
- `skills/custom/shortform-copywriting.md` 추출 완료 — 26개 원칙 (단축 규칙 10, 공식템플릿 5, 해외캠페인 11)
- `skills/marketing/` 디렉토리에 13개 마케팅 스킬 원본 보유

### Active (이 마일스톤) — v1 완료 (2026-08-01)
- [x] P1: 전략 제안서 생성기 UI 골격 — 2패널 레이아웃, 다크테마, **10개 입력 필드**, 결과 영역 (바닐라 JS) — **[검증됨]** 2026-08-01: Phase 1~6 통합 E2E(수동/자동 모드) + 프로덕션 배포 후 브라우저 동작 확인
- [x] P2: 기획안 생성 로직 + 결과 렌더링 + PDF — 템플릿 기반 대본 생성(제안서의 구성요소로), jsPDF, 복사/새로 만들기 — **[검증됨]** 위와 동일
- [x] P3: **당위성 엔진 + 설득형 제안서 문서화** ⭐ — 제품별 맞춤 근거 생성, 문제진단→전략→크리에이티브→기대효과→원칙부록 순서 제안서 PDF — **[검증됨]** 위와 동일
- [x] P4: Claude API 자동화 — 서버사이드 API 연동, 자동 모드에서 당위성 엔진 진가 발휘 — **[검증됨]** 2026-08-01: 자동 모드 프로덕션 E2E 90초 생성 + PDF 다운로드 확인. 무료 우선 5단계 폴백 체인(commit 347222c)
- [x] P5: 영상 소스 생성기 — 내부용 재료 도구, 씬 파싱, EN 프롬프트, 상세도 조절, 카피 버튼 (UI 간소화) — **[검증됨]** 2026-08-01: 통합 E2E + Phase 7 UAT 회귀(기존 2탭 무손상 10/10)
- [x] P6: 두 도구 연결 + 배포 — 탭 전환, "2번으로 보내기" 자동 전달, Vercel 배포, 통합 E2E 테스트 — **[검증됨]** 2026-08-01: Vercel 배포 완료(https://ad-script-studio.vercel.app) + 프로덕션 E2E + PDF 한글 폰트 최적화(v1.0.1) 배포 후 재검증

### v1 이후 추가 Phase (Phase 7 — 벤치마킹 분석기, 2026-08-01 추가)
- [x] P7: 벤치마킹 대본 분석기 — IG 계정 입력 → 크롤링 → 바이럴 필터링 → Whisper 전사 → 구조 해부 → 새 대본 재조립 (Job 폴링 + KV 스테이지 머신 + 서버 비용 상한) — **[검증됨]** 2026-08-01: UAT 12/12 통과 (실제 gymshark 계정 E2E 포함, .planning/phases/07-benchmark-analyzer/07-UAT.md). 단, 외부 API(Apify/OpenAI) 의존 검증은 키 설정 환경에서만 수행됨

## Success Criteria

| 기준 | 측정 방법 |
|------|----------|
| 전략 제안서 생성기가 제품별 맞춤 당위성 근거를 생성할 수 있다 | "왜 이 원칙이 필요한가" 근거 문장이 입력값 기반으로 표시됨 |
| 제안서 PDF가 설득 논리의 흐름을 따른다 | 문제진단→전략및근고→크리에이티브→기대효과→원칙부록 순서로 렌더링 |
| 수동 모드에서 정형 근거가 표시된다 | 당위성 근거 카드에 원칙명 + 간단 근거 표시 |
| 자동 모드에서 논리적 근거가 생성된다 | Claude API 연동 시 입력값과 원칙을 연결한 논리적 서술 표시 |
| 영상 소스 생성기가 씬별 EN 프롬프트를 정확히 생성한다 | EN 프롬프트 카피 버튼으로 복사 가능 |
| 두 도구가 연결되어 기획안 → 영상 소스로 자동 전달된다 | "2번으로 보내기" 버튼 클릭 시 자동 전달 |
| Vercel에 배포되어 접근 가능하다 | 배포 URL로 접속하여 전체 플로우 동작 확인 |

## Scope

### In Scope
- 전략 제안서 생성기 (Strategic Proposal Generator)
- **당위성 엔진** — 제품별 맞춤 근거 생성 (수동 정형 문구 + 자동 API 논리)
- **설득형 제안서 PDF** — 문제진단→전략및근거→크리에이티브→기대효과→원칙부록
- 영상 소스 생성기 (내부용 재료 도구)
- 두 도구 간 탭 전환 및 자동 전달
- 스킬 지침 (26원칙) 시스템 프롬프트 주입
- 클라이언트 제안용 PDF 다운로드 (일반PDF + 제안서PDF)
- Vercel 배포 (서버리스 함수 포함)
- 수동 복붙 모드 → API 자동화 모드 점진적 전환

### Out of Scope
- 영상 편집 도구 연동
- 사용자 인증/로그인
- 결제 시스템
- 데이터베이스 저장 (클라이언트 사이드)
- 모바일 네이티브 앱

## Constraints

- **프론트엔드**: 바닐라 HTML/CSS/JS (React, Next.js 등 프레임워크 사용 안 함)
- **API 키 보안**: Anthropic API 키는 서버 사이드(Vercel 서버리스)에서만 처리, 프론트엔드에 노출 금지
- **배포**: Vercel (serverless functions + static hosting)
- **언어**: 대본 생성은 한국어, 영상 소스 프롬프트는 영어 (EN)
- **스킬 소스**: `skills/custom/shortform-copywriting.md` (26개 원칙) — 런타임에 서버리스 함수가 읽어 시스템 프롬프트에 주입

## Current Milestone: v2.0 원소스 멀티유즈 콘텐츠 시스템

**Goal:** 하나의 콘텐츠 코어(상품/주제 정보 + 당위성 근거 26개 원칙)로부터 쇼츠·카드뉴스·인포그래픽·롱폼 4개 포맷을 생성하는 원소스 멀티유즈 시스템으로 확장. v1 전략 제안서 생성기는 무손상 유지.

**Target features:**
- 콘텐츠 코어 YAML 스키마 (7그룹 30+ 필드, campaignId 기반 저장)
- 쇼츠 렌더러 (대본 → 씬 파싱 → 이미지 → TTS → 렌더링 준비 완료, vox-content 파이프라인 계승)
- 카드뉴스 + 인포그래픽 렌더러 (슬라이드/데이터 출력 + 제휴 고지 + 과장 플래그)
- 롱폼 렌더러 + 주제 브릿지 (mc 깊이단계 모델 + mc 블로그 체인 주제 조회/적재)
- 렌더링 파이프라인 완성 (Whisper 자막 + moviepy + 썸네일, Job-status 폴링)
- 법적 컴플라이언스 + 과장 필터 (4포맷 고지·자막 규칙·필터·니치 규칙)

**Research:** 완료 (STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md — `.planning/research/`)

## Milestones

| # | Milestone | 상태 |
|---|-----------|------|
| 1 | AD SCRIPT STUDIO v1 (Phase 1~7) | ✅ **완료** (2026-08-01) — 프로덕션 배포 + E2E + PDF 한글 폰트 최적화 포함 |
| 2 | v2.0 원소스 멀티유즈 콘텐츠 시스템 | 🟡 계획 중 (2026-08-09 ~ ) — 리서치 + 요구사항 정의 완료, ROADMAP 작성 전 |

## Notes

- 이 프로젝트는 mde2(Hugo 퍼블리셔)와 별도 독립 프로젝트
- `skills/marketing/`은 marketingskills 저장소 클론 (coreyhaines31)
- `skills/custom/shortform-copywriting.md`는 이전 세션에서 3개 카테고리(훅, CTA, 심리적 트리거)로 추출 완료
- **리팩터링 (2026-07-31):** 도구 1의 정체성을 '대본 생성기'에서 '전략 제안서 생성기'로 재정의. Phase 3(당위성 엔진 + 제안서 문서화)를 프로젝트 핵심으로 격상. 영상 소스 생성기는 내부용 재료 도구로 격하.
- **핵심 변경 사유:** 기존 5개 입력 필드로는 당위성을 만들어낼 근거가 부족. 리뷰 발췌, 경쟁사 차이, 가격대 정보가 있어야 Phase 3의 당위성 엔진이 진짜 근거 있는 문장을 만들 수 있음. 반대로 영상 소스 생성기는 사용자 본인만 보는 화면이므로 UI 완성도에 시간을 쓸 필요 없음.
- **v1 최종 검증 (2026-08-01):** PDF 한글 폰트 최적화(CDN 10.4MB 변수 폰트 → 로컬 서브셋 1.1MB base64, 10초 타임아웃 폴백) 후 프로덕션 E2E 재검증 완료. PDF 817KB→316KB (61% 감소). v1.1 후보는 BACKLOG.md 참조.
